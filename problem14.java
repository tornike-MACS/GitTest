import acm.graphics.*;
import acm.program.GraphicsProgram;
import java.awt.*;

public class problem14 extends GraphicsProgram {
	public void run(){
		GRect head = new GRect(300, 400);
		head.setFilled(true);
		head.setColor(Color.RED);
		add(head, 200, 200);
		
	}
}
